Bigos team DoR:

The user story and/or requirement is clearly articulated, concise, and understandable to all team members. Concrete and measurable.

We have clear & testable Acceptance Criteria (it should contain information about /feature flag + expected release env).

Mockups are provided by UX/UI team if they are required. / If task is related to documentation there is necessity to define templates or common look of document with stakeholders (PO).

All necessary documents are provided and the related to tasks TechSpecs have been approved by local architect.

Task was estimated by the team. Story points are assigned.

All known dependencies of tasks and blockers are defined and resolved. (inside and outside)

Story is decomposed into smaller chunk under a structure with FE & BE subtasks by Scrum Team.

---

Zurek team DoR:

All dependencies of tasks and blockers are defined and resolved (inside and outside). Reference data sources should be provided if exists. Access should be ready before picking a story/task (For example SECO approvals). Except tasks to grant access etc.. This kind of tasks can take more than one sprint.

We need define acceptance crtieria

The task was well described and estimated by the team. Story points are assigned. Task should be understandable for erveryone.

Techscpec should be approved by ARB before realizing tasks (if project required techspec)